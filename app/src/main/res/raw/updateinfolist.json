{
  "$schema": "http://json-schema.org/draft-04/schema#",

  "description": "This schema represents catalog update information. It has a series of update information points in a list of the form [{point1}, ..., {pointn}]  where each {point} list is {timepoint, version} for now",

  "type": "array",
  "items": {
    "type": "object",
    "properties": {

      "timepoint":{
        "description": "The timepoint of a catalog with newer version.",
        "type": "number"
      },

      "version":{
        "description": "the version of the catalog",
        "type": "number"
      }
    },

    "required": [
      "timepoint",
      "version"
    ]
  }
}